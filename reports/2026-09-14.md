# 每日构建报告 2026-09-14

## 1. 发布
发布新文章: 室外设计要点：庭院、门头与外立面最容易踩的坑 (2026-09-14)
构建站点 -> F:\guangergaozhi\开发\每日定时触发\output
  生成文章: ai-design-workflow.html
  生成文章: ai-design-to-landing.html
  生成文章: interior-design-basics.html
  生成文章: exterior-design-guide.html
  生成: index.html / sitemap.xml / feed.xml / robots.txt

## 2. 构建
构建站点 -> F:\guangergaozhi\开发\每日定时触发\output
  生成文章: ai-design-workflow.html
  生成文章: ai-design-to-landing.html
  生成文章: interior-design-basics.html
  生成文章: exterior-design-guide.html
  生成: index.html / sitemap.xml / feed.xml / robots.txt

## 3. 同步 GitHub
[main 515f018] chore: daily publish 2026-09-14
 1 file changed, 1 insertion(+), 1 deletion(-)
推送失败（直连 + 代理均失败）：schannel: failed to receive handshake, SSL/TLS connection failed
随后定位并修复：本机 Git for Windows 默认 TLS 后端是 schannel，走 HTTP 代理时握手必失败；
在 git_via_proxy() 内增加 `-c http.sslBackend=openssl` 后，代理推送恢复正常。
补充提交：
- [main e6a3894] feat(daily_run): 直连失败自动回退到系统代理重试，支持 GIT_PROXY 自定义端口
- [main ade4641] feat(daily_run): 代理回退增加 http.sslBackend=openssl

## 4. 知乎稿
`zg_draft.py` 已新增（健壮版），为已发布文章生成知乎版稿件输出到 zhihu/ 目录，
文末原文链接保留用于把权重导回自家站。本次一并提交推送。

## 5. 选题库存
topics.json 共 30 篇 / 已发 4 篇 / 剩余 26 篇，按每天 1 篇可撑约 26 天。

## 6. 企业微信
- 推送必须用 node 调真实入口（本机 wecom-cli shim 在 Git Bash 下路径会被破坏）。
- chat_id = 授权真人 ID wozNxoEQAAgOlLCTn45hIm0Z-MudbD1g（identity whoami 取得）。
- 今日日报待推送。

## 7. 本次踩坑沉淀
- **git push 三重障碍**（本机 Windows 沙箱）：
  1. 直连 GitHub 被重置 → 必须用系统代理 http://127.0.0.1:7897
  2. schannel 后端走代理 TLS 握手失败 → 必须加 -c http.sslBackend=openssl
  3. wecom-cli shim 在 Git Bash 下路径损坏 → 必须用 node 调真实 JS 入口
- shell 里写长 python -c 时，务必用英文单引号包裹 content，否则 bash 会拆参数报 command not found。
